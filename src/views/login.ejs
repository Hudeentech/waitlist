<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: '#52AF44',
            }
          }
        }
      }
    </script>
  </head>
  <body class="bg-gradient-to-tr from-[#52AF44] to-green-800">
    <div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
      <div class="max-w-md w-full space-y-8">
        <div>
          <div class=" mx-auto rounded-full flex items-center justify-center">
              <img class="w-[100px]" src="/images/logo.png" alt="img">
          </div>

          <h2 class="mt-6 text-center text-3xl font-semibold text-white">Waitlist Admin</h2>
          <p class="mt-2 text-center text-sm text-gray-200">Sign in to manage your subscribers</p>
        </div>

        <div class=" p-4">
          <% if (error && error.length) { %>
            <div class="mb-6 p-4 rounded-md bg-red-50 border border-red-200">
              <div class="flex">
                <i class="fas fa-exclamation-circle text-red-400 mt-0.5"></i>
                <p class="ml-3 text-red-700"><%= error %></p>
              </div>
            </div>
          <% } %>

          <form method="post" action="/admin/login" class="space-y-6">
            <div>
              <label class="block text-sm font-medium text-gray-100">
                <i class="fas fa-user text-gray-100 mr-1"></i>
                Username
              </label>
              <input name="username" required autofocus placeholder="Enter your username"
                class="mt-1 block w-full px-3 py-3 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-primary focus:border-primary" />
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-100">
                <i class="fas fa-lock text-gray-100 mr-1"></i>
                Password
              </label>
              <input name="password" type="password" required placeholder="Enter master's password"
                class="mt-1 block w-full px-3 py-3 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-primary focus:border-primary" />
            </div>
            <div>
              <button type="submit"
                class="w-full flex items-center justify-center py-3 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-primary hover:bg-primary/90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors">
                <i class="fas fa-sign-in-alt mr-2"></i>
                Sign in
              </button>
            </div>
          </form>
        </div>
        
        <div class="text-center text-sm text-gray-100 rounded-xl m-2 p-4 border">
          <i class="fas fa-info-circle text-gray-410 mr-1"></i>
          Please Note: <i>this Admin panel belongs to KORIM TECHNOLOGY to login please use your username and master's password</i>
        </div>
      </div>
    </div>
  </body>
</html>
