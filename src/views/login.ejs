<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: '#52AF44',
            }
          }
        }
      }
    </script>
  </head>
  <body class="bg-gray-50">
    <div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
      <div class="max-w-md w-full space-y-8">
        <div>
          <div class="w-16 h-16 mx-auto bg-primary rounded-full flex items-center justify-center">
            <i class="fas fa-envelope text-3xl text-white"></i>
          </div>
          <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">Waitlist Admin</h2>
          <p class="mt-2 text-center text-sm text-gray-600">Sign in to manage your subscribers</p>
        </div>

        <div class="bg-white p-8 rounded-xl shadow-sm border border-gray-100">
          <% if (error && error.length) { %>
            <div class="mb-6 p-4 rounded-md bg-red-50 border border-red-200">
              <div class="flex">
                <i class="fas fa-exclamation-circle text-red-400 mt-0.5"></i>
                <p class="ml-3 text-red-700"><%= error %></p>
              </div>
            </div>
          <% } %>

          <form method="post" action="/admin/login" class="space-y-6">
            <div>
              <label class="block text-sm font-medium text-gray-700">
                <i class="fas fa-user text-gray-400 mr-1"></i>
                Username
              </label>
              <input name="username" required autofocus
                class="mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-primary focus:border-primary" />
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700">
                <i class="fas fa-lock text-gray-400 mr-1"></i>
                Password
              </label>
              <input name="password" type="password" required
                class="mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-primary focus:border-primary" />
            </div>
            <div>
              <button type="submit"
                class="w-full flex items-center justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-primary hover:bg-primary/90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary transition-colors">
                <i class="fas fa-sign-in-alt mr-2"></i>
                Sign in
              </button>
            </div>
          </form>
        </div>
        
        <div class="text-center text-sm text-gray-500">
          <i class="fas fa-info-circle text-gray-400 mr-1"></i>
          Default login: <code class="px-2 py-1 bg-gray-100 rounded">enter admin details</code> / <code class="px-2 py-1 bg-gray-100 rounded">enter mater password</code>
          <div class="mt-1 text-gray-400">(change in .env)</div>
        </div>
      </div>
    </div>
  </body>
</html>
